import { FlatList, View, Text } from 'native-base';
//import { FlatList, View, Text } from 'react-native';
export default function JavaScriptProjects({ navigation, channel }) {
    return (
        <View p='1'>
            <Text color={'pink'}>script</Text>
        </View>
    );
}